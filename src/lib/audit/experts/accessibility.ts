import { expertPrompt } from "./shared";
export const accessibilityPrompt = expertPrompt("accessibility", "Accessibility Specialist", "Lighthouse evidence, semantics, keyboard access, forms, contrast, alternative text and accessible landmarks");
